import * as React from "react";

const history = props => {

    return (

        <div>
            <h1>{ chrome.i18n.getMessage("l10nHistory") }</h1>
        </div>
    )

}

export default history;